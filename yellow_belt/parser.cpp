#include <iostream>

using namespace std;

class Node{
public:
    int Evalute(){ return 0;};
};

Node Parse(const string& tokens, int& x){

}

int main(){
    cout << "Enter expresion: ";
    string tokens = "5+7-x*x+x";
//    cin >> tokens;
//    cout << "Enter x: ";
    int x;
//    auto expr = Parse(tokens, x);
    while (cin >> x){

        cout << expr->Evalute() << endl;
    }

    return 0;
}